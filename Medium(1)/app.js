function vowelChecker(x){ // created function to locate vowels
    var firstChar = x.toLowerCase(); // this will evaluate the first letter as a lower case letter
      charAt(0);
     if (firstChar === "a" || firstChar === "e" || firstChar === "i" || firstChar === "o" || firstChar === "u") { // this specifies the vowels we are looking for
        console.log("This is a vowel"); // if it is a vowel then this will be returned
     }
     else {
      console.log("This is not a vowel"); // if it is not a vowel then this will be returned
     }
  }